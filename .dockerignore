# Keep the build context and image lean and credential-free. The Dockerfile also
# copies an explicit allowlist of paths, so this is defense in depth.
node_modules
.git
.gitignore
test
**/*.test.js
.forgejo
scripts
README.md
.env
.env.example
cordis.patch.yml
Dockerfile
.dockerignore
npm-debug.log*
